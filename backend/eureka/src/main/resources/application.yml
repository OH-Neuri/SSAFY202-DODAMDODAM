#포트번호
server:
  port: 8761

#고유 아이디 부여
spring:
  application:
    name: eureka

#eureka서버 스스로를 eureka에 등록하는걸 방지
#기본값은 원래 true
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false