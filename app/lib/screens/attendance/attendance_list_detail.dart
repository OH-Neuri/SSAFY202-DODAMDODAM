import 'package:flutter/material.dart';

class AttendaneListDetail extends StatelessWidget {
  const AttendaneListDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
