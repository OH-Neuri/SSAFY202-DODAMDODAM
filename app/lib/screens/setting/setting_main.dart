import 'package:flutter/material.dart';

class SettingMain extends StatelessWidget {
  const SettingMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("환경설정 페이지여"),
    );
  }
}
